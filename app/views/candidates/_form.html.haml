= form_for @candidate do |f|

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description, :rows => 5, :class => 'input-xxlarge'
  .field
    = f.label :image_url
    = f.text_field :image_url
  .actions
    = f.submit 'Save'
