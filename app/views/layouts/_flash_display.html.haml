- flash.each do |key, value|
  %div{:class => (flash_class(key))}
    /= safe_join(value, "<br>".html_safe)
    - if value.is_a? String
      = value
    - else
      = safe_join(value, "<br>".html_safe)